type ButtonSize = 'lg' | 'md' | 'sm' | 'xs';
type ButtonColor = 'primary' | 'secondary' | 'tertiary';

type ButtonProps = {
  size: ButtonSize;
  color: ButtonColor;
};

export type { ButtonSize, ButtonColor, ButtonProps };
